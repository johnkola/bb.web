apiVersion: v1
kind: Service
metadata:
  name: {{ include "eih-base-helm.fullname" . }}
  labels:
    {{- include "eih-base-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eih-base-helm.selectorLabels" . | nindent 4 }}
