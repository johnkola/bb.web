apiVersion: v1
kind: Service
metadata:
  name: {{ include "bb-web.fullname" . }}
  labels:
    {{- include "bb-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "bb-web.selectorLabels" . | nindent 4 }}
